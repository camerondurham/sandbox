const SIEVE_MIN: usize = 1;
const SIEVE_MAX: usize = 1_000_000;
fn main() {
    println!("Hello, world!");
    let mut sieve = [true;SIEVE_MAX];
    for i in 2..1000 {
        let isPrime = true;
        for j in sievev.
    }
}

/*
    primeList = []
    for i in range(2, s):
        isPrime = True
        for j in primeList:
            if i % j == 0:
                isPrime = False
                break
            if j > math.sqrt(i):
                break
        if isPrime:
            primeList.append(i)
    return primeList
*/
